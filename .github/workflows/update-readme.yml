name: Update README

on:
  schedule:
    - cron: '*/30 * * * *'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    name: Update this repo's README with recent activity

    steps:
      - uses: actions/checkout@v2
      - uses: rifqifarhansyah/github-activity-readme@master
        env:
          GITHUB_TOKEN: ${{ secrets.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 }}
